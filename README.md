# wireguard-hub

The WireGuard hub: brings the interface up from the peer table, serves the bundle version endpoint and writes the heartbeat. Design: `docs/superpowers/specs/2026-09-14-hub-fetched-peer-config-design.md`, section 3.
